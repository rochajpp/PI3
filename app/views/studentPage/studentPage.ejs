<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/studentPage/style.css">
    <title>Página do Estudante - <%= aluno[0].matricula%></title>
</head>
<body>
    <h1>Bem-vindo(a) ao System Dev!</h1>
    <h3>Aluno: <%= aluno[0].nome %> </h3>
    <h4>Mátricula: <%= aluno[0].matricula %> </h4>
    <h2>Lista de Disciplinas:</h2>
    <table>
      <thead>
        <tr>
          <th>Código</th>
          <th>Disciplina</th>
          <th>Professor(a)</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% if(disciplinas.length != null){ %>
            <% for(var i = 0; i < disciplinas.length; i++){ %>
                <tr onclick="location.href='subject?disciplina=<%= disciplinas[i].id %>&aluno=<%= aluno[0].matricula %>&password=<%= pass %>'">
                  <td>0<%= i + 1 %></td> 
                  <td><%= disciplinas[i].nome %></td>     
                  <td><%= disciplinas[i].professor %></td>        
                  <td class="buttons">
                    <a href="/alterarDisciplina?disciplina=<%= disciplinas[i].id %>&aluno=<%= aluno[0].matricula %>"><button>Alterar</button></a>
                    <a href="deletarDisciplina?disciplina=<%= disciplinas[i].id %>&aluno=<%= aluno[0].matricula %>"><button>Deletar</button></a>
                  </td>                 
               </tr>              
            <%}%>
        <%}%>
        
        
      </tbody>
      
    </table>
    <a class="add" href="addSubject?matricula= <%= aluno[0].matricula %>" >+</a>
</body>
</html>